<script>
  <% if @notice %>
    $('#toast').toast({
      delay: 500
    })
  <% end %>

  let screenWidth = window.innerWidth;
  let screenHeight = window.innerHeight;

  

  // Check if mobile device
  if( screen.width <= 480 ) {     
    // mobile
    window.MOBILE = true;
  } else {
    window.MOBILE = false;
  }

  // mobile일 경우, 네브바의 알림 상태를 바꾼다.
  // if(!window.MOBILE){
  //   $('#notification-count').css({
  //     'display' : 'flex',
  //     'right': '-1px',
  //     'top' : '30px',
  //     'position' : 'absolute'
  //   })
    
  // } else {
    
  // }
</script>