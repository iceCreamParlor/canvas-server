<!-- Home section -->
<div class="section-lg bg-image bg-image-gradient">
  <div class="bg-white-08">
    <div class="container" style="padding-bottom: 0px;">
      <div class="row align-items-center col-spacing-50">
        <div class="col-12 col-lg-4">
          <% if @user.image.present? %>
            <img class="box-shadow-with-hover" src="<%= @user.image.url %>" alt="">
          <% end %>
        </div>
        
        <div class="col-12 col-lg-3">
          
          <h2 class="font-weight-normal">
            <%= @user.nickname %>
            <% if user_signed_in? && @user.id == current_user.id %>
              <%= link_to edit_profile_path(@user.id) do %>
                <button type="button" class="btn btn-outline-dark profile-edit-btn">수정</button>
              <% end %>
            <% end %>
          </h2>
          <ul class="list-horizontal-dash margin-bottom-20">
            <% @user_category.each do |category| %>
              <li class="heading-uppercase text-dark"><%= category.name %></li>
            <% end %>
          </ul>
          <ul class="list-horizontal-unstyled margin-bottom-20">
            <!-- <li><a href="#"><i class="fab fa-facebook-f"></i></a></li> -->
            <!-- <li><a href="#"><i class="fab fa-twitter"></i></a></li> -->
            <!-- <li><a href="#"><i class="fab fa-pinterest"></i></a></li> -->
            <% if @user.instagram.present? %>
              <li>
                <%= link_to "#{ @user.instagram }", target: "_blank", rel: "noopener noreferrer" do %>
                  <i class="fab fa-instagram"></i>
                <% end %>
              </li>
            <% end %>
            
          </ul>
          <div class="row margin-top-area">
            <div class="col-4">
              <span class="profile-text">게시물 <%= @user.paintings.count %></span>
            </div>
            <div style="text-align: center;" class="col-4" data-toggle="modal" data-target="#followerModal">
              <span class="profile-text">팔로워 <%= @user.followed.count %></span>
            </div>
            <div class="col-4" data-toggle="modal" data-target="#followingModal">
              <span style="float: right;" class="profile-text">팔로우
                <span id="follows-count">
                  <%= @user.follows.count %>
                </span>
              </span>
            </div>
            
          </div>
          <div class="margin-top-middle">
            <% if @user.desc.present? %>
              <p><%= @user.desc %></p>
            <% end %>
          </div>
          <!-- Skills -->
          <!-- <div class="row margin-top-30">
            <div class="col-12 col-lg-6">
              <div class="progress-box">
                              <h6 class="font-weight-normal">Photography</h6>
                              <div class="progress">
                                  <div class="progress-bar" role="progressbar" style="width: 90%;" aria-valuenow="90" aria-valuemin="0" aria-valuemax="100">
                                      <span>90%</span>
                                  </div>
                              </div>
                          </div>
            </div>
            <div class="col-12 col-lg-6">
              <div class="progress-box">
                              <h6 class="font-weight-normal">Marketing</h6>
                              <div class="progress">
                                  <div class="progress-bar" role="progressbar" style="width: 80%;" aria-valuenow="80" aria-valuemin="0" aria-valuemax="100">
                                      <span>80%</span>
                                  </div>
                              </div>
                          </div>
            </div>
            <div class="col-12 col-lg-6">
              <div class="progress-box">
                              <h6 class="font-weight-normal">Branding</h6>
                              <div class="progress">
                                  <div class="progress-bar" role="progressbar" style="width: 85%;" aria-valuenow="85" aria-valuemin="0" aria-valuemax="100">
                                      <span>85%</span>
                                  </div>
                              </div>
                          </div>
            </div>
            <div class="col-12 col-lg-6">
              <div class="progress-box">
                              <h6 class="font-weight-normal">Drawing</h6>
                              <div class="progress">
                                  <div class="progress-bar" role="progressbar" style="width: 95%;" aria-valuenow="95" aria-valuemin="0" aria-valuemax="100">
                                      <span>95%</span>
                                  </div>
                              </div>
                          </div>
            </div> -->
          </div><!-- end inner row -->
        </div>
        <div class="col-12 col-lg-5"></div>
      </div><!-- end row -->
    </div><!-- end container -->
  </div>
</div>
<!-- end Home section -->

<!-- Portfolio section -->
<div class="section-lg bg-grey-lighter profile-portfolio margin-top-area">
  <div class="container">
    <div class="section-title" style="margin-bottom: 20px;">
      <h2 class="font-weight-normal">Artworks</h2>
      <div class="divider-zigzag divider-zigzag-color-black-07">
        <span></span><span></span><span></span>
      </div>
    </div>
    <!-- Portfolio filter -->
    <% if @user.paintings.present? %>
      <div class="filter filter-style-4 margin-bottom-50">
        <ul>
          <li class="active" data-filter="*">All</li>
         
          <% @user_category.each_with_index do |category, index|%>
            <li data-filter=".category-<%= index %>"><%= category.name %></li>
          <% end %>
        </ul>
      </div>
      <!-- end Portfolio filter -->
      <div class="portfolio-wrapper column-3 spacing-10">
        <!-- Portfolio box 1 -->
        <% @user_category.each_with_index do |category, index|%>
          <% category.filter_paintings(user: @user).each do |painting| %>
            <%= link_to painting_path(painting) do %>
              <div class="portfolio-item category-<%= index %>">
                <div class="portfolio-box">
                  <div class="portfolio-img">
                    <img src="<%= painting.thumbnail.url %>" alt=""><!-- Image source -->
                  </div>
                  
                  <div class="portfolio-title">
                    <div>
                      <h5 class="font-weight-normal"><%= painting.name %></h5>
                      <span><%= category.name %></span>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>
      </div><!-- end portfolio-wrapper-->
    <% end %>
  </div><!-- end container -->
</div>
<!-- end Portfolio section -->

<%= render 'layouts/follower_modal' %>
<%= render 'layouts/following_modal' %>
<script>
  function followUser(element){
    <% if user_signed_in? %>
      let list = element.closest('.list-group-item');
      
      let isFollowing = element.dataset.following == 'true';
      let dataUser = parseInt(element.dataset.user);
      
      let followerId = <%= current_user.id %>;
      let followeeId = dataUser;

      
      $.ajax({
        url: '/follow/' + followerId + '/' + followeeId,
        method: 'GET',
        success: function(){
          if(!isFollowing){
            // 원래 상태가 언팔로우인 경우
            console.log('follow')
            element.dataset.following = 'true';
            element.innerText = '언팔로우';
            
          } else {
            console.log('unfollow')
            element.dataset.following = 'false';
            element.innerText = '팔로우';
          }
        }
      })
    <% end %>
  }
</script>