let newComment = `
<div class="comment-box">
  <div class="comment-user-avatar">
    <%  if @painting_comment.user.image.present? %>
      <img class="magazine-comment-image" src="<%= @painting_comment.user.image.url %>" alt="">
    <% else %>
      <i class="fa fa-user"></i>
    <% end %>
  </div>
  <div class="comment-content">
    <span class="comment-time"><%= @painting_comment.created_at.strftime("%Y년 %m월 %d일") %></span>
    <h6 class="font-weight-normal"><%= @painting_comment.user.nickname %></h6>
    <p>
      <%= @painting_comment.content %>
      <%= link_to painting_comment_path(@painting_comment), 
        method: :delete, 
        data: {
          "remote": "true"
        } do 
      %>
        <button style="font-style: italic;" class="float-right">삭제</button>
      <% end %>
    </p>
  </div>
</div>
`
console.log("!!!")
$('#comment-area').append(newComment);
$('#comment').val('');